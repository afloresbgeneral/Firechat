// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  firebase: {
    apiKey: 'AIzaSyAH4E1v4Vv-MR3FK_8PbZeb-nsPyMBYUj4',
    authDomain: 'firechat-477ec.firebaseapp.com',
    databaseURL: 'https://firechat-477ec.firebaseio.com',
    projectId: 'firechat-477ec',
    storageBucket: '',
    messagingSenderId: '174190766545',
    appId: '1:174190766545:web:ae5a74de26793d452718f0'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
